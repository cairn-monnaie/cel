{# src/Cairn/UserBundle/Resources/views/Banking/operation_confirm.html.twig #}

{% extends "CairnUserBundle::layout.html.twig" %}

{% block stylesheets %} {{parent()}} {% endblock %}
{% block body %}
        {{parent()}}
    <h1>Récapitulatif du paiement en cours </h1>
    
    <li>
        {% if operationReview.fromAccount.owner.display is defined %}
            {{ operationReview.fromAccount.owner.display }}
        {% else %}
            Association Le Cairn
        {% endif %}
        {{ operationReview.fromAccount.type.name }}
        {{ operationReview.fromAccount.number }}

    </li>
    <li>
        {% if operationReview.toAccount.owner.display is defined %}
            {{ operationReview.toAccount.owner.display }}
        {% else %}
            Association Le Cairn
        {% endif %}
        {{ operationReview.toAccount.number }}
    </li>
    <li>{{ operationReview.totalAmount.amount }}</li>
    
    <div class="well">
      {{ form(form) }}
    </div>
{% endblock %}

{% block javascripts %}

{% endblock %}
